// default-parameters.1.js
function render (fogLevel = 0, sparkLevel = 100) {
  console.log(`Fog Level: ${fogLevel} and spark_level: ${sparkLevel}`)
}
render(10) // Fog Level: 10 and spark_level = 100
